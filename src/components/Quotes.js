import React from 'react';
import style from './style/quotes.module.css';

const Quotes = () => (
  // eslint-disable-next-line max-len
  <h1 className={style.quote}>A language that does not affect the way you think about programming is not worth knowing.</h1>
);

export default Quotes;
